<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.chathu.georoam">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.HomeScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.RegisterActivity"
            android:label="Register" />
        <activity
            android:name=".view.RegisterFinalActivity"
            android:label="RegisterFinal" />
        <activity
            android:name=".view.LoginActivity"
            android:label="Login" />
        <activity
            android:name=".view.GetStartedActivity"
            android:label="GetStarted" />
        <activity
            android:name=".view.DashboardActivity"
            android:label="Dashboard" />
        <activity
            android:name=".view.ProfileActivity"
            android:label="Dashboard" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".view.SearchMapActivity"
            android:label="@string/title_activity_search_map"/>
        <activity
            android:name=".view.AddPostActivity"
            android:label="AddPost" />
        <activity
            android:name=".view.AddPictureTextActivity"
            android:label="AddPictureText" />
        <activity
            android:name=".view.AddPictureMapActivity"
            android:label="AddPictureMap" />
        <activity
            android:name=".view.AddPictureImageActivity"
            android:label="AddPictureImage" />
        <activity
            android:name=".view.PicturePostedActivity"
            android:label="PicturePosted" />
        <activity
            android:name=".view.AddEventTextActivity"
            android:label="AddEventText" />
        <activity
            android:name=".view.AddEventMapActivity"
            android:label="AddEventMap" />
        <activity
            android:name=".view.AddEventImageActivity"
            android:label="AddEventImage" />
        <activity
            android:name=".view.EventPostedActivity"
            android:label="EventPosted" />
        <activity
            android:name=".view.MyPostsActivity"
            android:label="MyPosts" />
        <activity
            android:name=".view.MyPicturesActivity"
            android:label="MyPictures" />
        <activity
            android:name=".view.MyEventsActivity"
            android:label="MyEvents" />
    </application>

</manifest>